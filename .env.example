DATABASE_URL="file:./dev.db"

# Generate with: openssl rand -base64 32
AUTH_SECRET=""

# Google Cloud OAuth client (Web application)
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Email of the first user allowed to sign in. Leave blank to allow any Google
# account; once you've signed in, set this to your own email to lock it down.
ALLOWED_EMAIL=""
